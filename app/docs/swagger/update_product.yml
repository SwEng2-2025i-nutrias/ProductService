tags:
  - Products
summary: 'Actualizar un producto existente'
description: 'Actualiza un producto existente con los datos proporcionados. Todos los campos son opcionales.'
parameters:
  - name: product_id
    in: path
    type: integer
    required: true
    description: 'ID del producto a actualizar'
    example: 1
  - name: body
    in: body
    required: true
    schema:
      type: object
      properties:
        name:
          type: string
          description: 'Nombre del producto'
          example: 'Tomates Cherry Orgánicos'
        farm_id:
          type: integer
          description: 'ID de la granja'
          example: 1
        type:
          type: string
          description: 'Tipo de producto'
          example: 'Vegetal Orgánico'
        quantity:
          type: integer
          description: 'Cantidad disponible'
          example: 150
        price_per_unit:
          type: number
          format: float
          description: 'Precio por unidad'
          example: 3.00
        description:
          type: string
          description: 'Descripción del producto'
          example: 'Tomates cherry orgánicos certificados'
        harvest_date:
          type: string
          format: date-time
          description: 'Fecha de cosecha (ISO format)'
          example: '2023-12-01T10:00:00Z'
responses:
  200:
    description: 'Producto actualizado exitosamente'
    schema:
      type: object
      properties:
        message:
          type: string
          example: 'Product updated'
  400:
    description: 'Datos de entrada inválidos'
    schema:
      type: object
      properties:
        error:
          type: string
          example: 'No data provided'
  404:
    description: 'Producto no encontrado'
    schema:
      type: object
      properties:
        error:
          type: string
          example: 'Product not found'
  500:
    description: 'Error interno del servidor'
    schema:
      type: object
      properties:
        error:
          type: string
